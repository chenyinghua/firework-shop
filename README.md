# 🎆 隆平烟花选购平台 (Fireworks Shop)

这是一个专为移动端优化的现代化烟花选购平台，采用纯原生前端技术 (HTML/CSS/JS) 构建，结合 Supabase 提供强大的后端数据支持。项目旨在提供极致的加载速度和流畅的用户体验。

## ✨ 核心功能特性

### 🛍️ 商品浏览与展示
*   **极致性能**: 采用 **骨架屏 (Skeleton Screen)** 预加载技术，拒绝白屏等待。
*   **流量节省**: 全站图片启用 **懒加载 (Lazy Loading)** 与异步解码，未浏览不消耗流量。
*   **沉浸式预览**: 点击商品图进入全屏预览模式，支持 **双指缩放/拖拽** (基于 `panzoom`)，查看烟花细节。
*   **双列布局**: 移动端专属优化的紧凑型双栏网格展示。
*   **实时热度**: 动态展示每款商品的“浏览量”与“选购量”，数据实时同步。

### 🔍 智能交互
*   **混合排序**: 独创的默认排序算法（加购量优先 -> 浏览量优先 -> 价格适中），也可按价格、热度手动排序。
*   **即时搜索**: 支持按商品名称实时过滤。
*   **二维码支持**: 每款商品支持独立二维码展示，适配不同尺寸屏幕扫描。

### 🛒 选货单 (购物车) 系统
*   **移动端适配**: 底部悬浮按钮 (FAB) 一键呼出侧边选货单。
*   **图文列表**: 购物车内包含商品缩略图、单位、单价及数量微调控件。
*   **专业单据生成**:
    *   一键生成**电子选货单**，界面模拟真实收据质感（红白蓝装饰条）。
    *   自动生成唯一订单编号 (`ORD-时间戳-随机码`)，高并发不重复。
    *   **长图导出**: 采用克隆渲染技术，无论订单多长，均可生成完整的**高清长图**保存到本地。
    *   **云端备份**: 订单数据自动同步至 Supabase 后台。

## 🛠️ 技术栈

*   **前端核心**: HTML5, CSS3 (Flex/Grid/Animations), Vanilla JavaScript (ES Modules).
*   **后端服务**: [Supabase](https://supabase.com) (PostgreSQL Database + Realtime API).
*   **关键库**:
    *   `panzoom`: 手势缩放与拖拽。
    *   `html2canvas`: 网页截图与图片生成。
    *   `FontAwesome`: 图标库。
*   **部署托管**: [Vercel](https://vercel.com) (全球 CDN 加速).

## 📂 项目结构

```text
├── public/              # 静态资源目录 (Vite 标准)
│   └── image/           # 商品图片与二维码
│       ├── commodity/   # 商品图
│       └── code/        # 二维码
├── index.html           # 应用入口与骨架屏结构
├── style.css            # 核心样式表 (包含响应式与动画)
├── app.js               # 业务逻辑 (数据交互/状态管理/渲染)
├── schema.sql           # 数据库初始化脚本
└── vercel.json          # 部署配置文件
```

## 🚀 快速部署指南

### 1. 准备 Supabase 数据库
1. 登录 Supabase 创建新项目。
2. 进入 **SQL Editor**，复制 `schema.sql` 内容并运行，创建以下表结构：
   *   `products`: 商品基础信息 (含 `unit` 单位字段)。
   *   `product_stats`: 浏览与加购统计。
   *   `orders`: 订单历史记录。
   *   **RPC 函数**: 用于原子化更新计数。

### 2. 本地运行
无需复杂的构建工具，直接启动即可（需配置环境变量）：

1.  **图片准备**: 将商品图片放入 `public/image/commodity/`，二维码放入 `public/image/code/`。
2.  **配置环境**: 
    *   复制 `.env.example` 为 `.env`。
    *   填入 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`。
3.  **启动**:
    ```bash
    npm install
    npm run dev
    ```

### 3. 部署到 Vercel (推荐)
1.  Fork 本仓库到你的 GitHub。
2.  在 Vercel 中 Import 项目，框架选择 **Vite**。
3.  **配置环境变量** (Settings -> Environment Variables):
    *   `VITE_SUPABASE_URL`: 你的 Supabase 项目 URL。
    *   `VITE_SUPABASE_ANON_KEY`: 你的 Supabase Public Key。
4.  点击 Deploy，等待烟花绽放！🎆

## 📱 兼容性
*   完美适配 iOS/Android 主流浏览器。
*   支持 PC 端响应式展示。
*   针对国内网络环境优化，使用 `unpkg` CDN 源。

---
**License**: MIT